{
    "name": "valorant-match-data-collection",
    "version": "1.0.0",
    "scripts": {
        "build": "mkdir -p dist && cp -f open-api-spec-entry.yaml dist/open-api-spec-entry.yaml && tsc --noEmit && ts-node ./esbuild.config.ts"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.188.0",
        "@aws-sdk/lib-dynamodb": "^3.188.0",
        "tweetnacl": "^1.0.3",
        "unofficial-valorant-api": "^2.3.1-stable.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.131",
        "@types/node": "^20.11.5",
        "esbuild": "^0.19.11",
        "prettier": "^3.2.4",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3",
        "yaml-cfn": "^0.3.2"
    }
}
