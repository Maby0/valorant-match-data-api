{
    "name": "valorant-match-data-api-internal",
    "version": "1.0.0",
    "scripts": {
        "build": "mkdir -p dist && tsc --noEmit && ts-node ./esbuild.config.ts"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.188.0",
        "@aws-sdk/lib-dynamodb": "^3.188.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.131",
        "@types/node": "^20.11.5",
        "esbuild": "^0.19.11",
        "prettier": "^3.2.4",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3",
        "yaml-cfn": "^0.3.2"
    }
}
